[project]
name = "sitemap-filter"
version = "0.1.0"
description = "Filter Alteryx help documentation sitemap by language and product"
requires-python = ">=3.10"
dependencies = [
]

[dependency-groups]
dev = []

[build-system]
requires = ["uv_build>=0.9.6,<0.10.0"]
build-backend = "uv_build"
