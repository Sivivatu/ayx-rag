[project]
name = "sitemap-download"
version = "0.2.0"
description = "Download Alteryx help sitemap with progress indication and validation"
requires-python = ">=3.10"
dependencies = [
]

[build-system]
requires = ["uv_build>=0.9.6,<0.10.0"]
build-backend = "uv_build"
